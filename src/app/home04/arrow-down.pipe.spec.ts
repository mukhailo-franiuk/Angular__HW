import { ArrowDownPipe } from './arrow-down.pipe';

describe('ArrowDownPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrowDownPipe();
    expect(pipe).toBeTruthy();
  });
});
